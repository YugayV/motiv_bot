# 🤖 Wisdom Daily Bot

Telegram бот для публикации мотивационных цитат в канал.

## 📋 Возможности

- ✅ Автоматическая публикация цитат по расписанию (9:00 и 21:00 МСК)
- 🎲 Получение случайных цитат по запросу
- 📚 Поиск цитат по категориям
- 🔍 Поиск по авторам и ключевым словам
- ❤️ Избранные цитаты
- 📊 Статистика использования
- 👨‍💼 Админ-панель для управления

## 🚀 Быстрый старт

### Вариант 1: Запуск через Docker (рекомендуется)

1. Убедитесь, что Docker установлен
2. Скопируйте `.env.example` в `.env` и заполните переменные:

   ```bash
   cp .env.example .env
   ```

3. Запустите бота:

   ```bash
   docker-compose up -d
   ```

### Вариант 2: Локальный запуск

1. Установите Python 3.11+
2. Установите зависимости:

   ```bash
   pip install -r requirements.txt
   ```

3. Создайте `.env` файл с вашими токенами (см. `.env.example`)
4. Запустите бота:

   ```bash
   python bot.py
   ```

## 🔧 Настройка

### Переменные окружения (.env)

```env
BOT_TOKEN=ваш_токен_бота_от_@BotFather
CHANNEL_ID=@ваш_канал_или_-100123456789
ADMIN_CHAT_ID=ваш_telegram_id
DEEPSEEK_API_KEY=ваш_api_ключ_deepseek
DEEPSEEK_API_URL=https://api.deepseek.com/v1/chat/completions
```

### Получение токенов

1. **BOT_TOKEN**: Получите от [@BotFather](https://t.me/BotFather)
2. **CHANNEL_ID**: ID вашего канала (начинается с `-100`)
3. **ADMIN_CHAT_ID**: Ваш Telegram ID (получите от [@userinfobot](https://t.me/userinfobot))
4. **DEEPSEEK_API_KEY**: Зарегистрируйтесь на [DeepSeek](https://platform.deepseek.com/)

## 📱 Команды бота

### Для пользователей

- `/start` - Запустить бота
- `/quote` - Получить случайную цитату
- `/categories` - Выбрать категорию
- `/search` - Поиск цитат
- `/stats` - Статистика бота
- `/favorites` - Избранные цитаты
- `/help` - Помощь

### Для админа

- `/admin` - Админ-панель
- `/force_post` - Опубликовать цитату сейчас
- `/add_quote` - Добавить новую цитату

## 🗂️ Структура проекта

```
motiv_bot/
├── bot.py                  # Основной файл бота
├── database.py             # Работа с базой данных
├── keyboards.py            # Клавиатуры для Telegram
├── deepseek_generator.py   # Генерация цитат через AI
├── requirements.txt        # Зависимости Python
├── Dockerfile             # Docker образ
├── docker-compose.yml     # Docker Compose конфигурация
├── .env                   # Переменные окружения (не в git)
└── .env.example          # Пример переменных окружения
```

## 🐳 Docker команды

```bash
# Запустить бота
docker-compose up -d

# Остановить бота
docker-compose down

# Посмотреть логи
docker-compose logs -f

# Перезапустить бота
docker-compose restart

# Пересобрать образ
docker-compose up -d --build
```

### 📜 Convenience Scripts (Windows)

Use these PowerShell scripts for managing the bot:

- `.\rebuild.ps1` - Rebuilds and restarts the container (recommended after updates)
- `.\run_local.ps1` - Runs the bot locally (requires installed dependencies)

## 📊 База данных

Бот использует SQLite базу данных `quotes.db` для хранения:

- Цитат и их метаданных
- Статистики использования
- Избранных цитат пользователей

## ⏰ Расписание публикаций

По умолчанию бот публикует цитаты:

- 9:00 МСК (06:00 UTC)
- 21:00 МСК (18:00 UTC)

Для изменения расписания отредактируйте строки 562-563 в `bot.py`.

## 🛠️ Разработка

### Добавление новых функций

1. Создайте новую ветку
2. Внесите изменения
3. Протестируйте локально
4. Создайте Pull Request

### Логирование

Логи сохраняются в консоль. Уровень логирования: `INFO`

## 📝 Лицензия

MIT License

## 🤝 Поддержка

При возникновении проблем создайте Issue в репозитории.

---

Сделано с ❤️ для распространения мудрости
